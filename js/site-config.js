// Site Configuration
// Single source of truth for domain, analytics, and environment settings.
// When migrating to a custom domain, update baseUrl and allowedHostnames here,
// then find-replace 'prpmdev.github.io/prashant-rana' in meta tags, sitemap, and robots.txt.

var SITE_CONFIG = {
    baseUrl: 'https://prpmdev.github.io/prashant-rana',
    ga: {
        measurementId: 'G-VS1PHJX1YL',
        allowedHostnames: ['prpmdev.github.io', 'localhost']
    }
};
